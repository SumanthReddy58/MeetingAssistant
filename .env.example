# Meeting Assistant Configuration

# Google OAuth & Calendar Integration
# 1. Go to Google Cloud Console (https://console.cloud.google.com/)
# 2. Create a new project or select existing one
# 3. Enable Google Calendar API
# 4. Create OAuth 2.0 credentials (Web application)
# 5. Add authorized redirect URIs: http://localhost:5173, http://localhost:3000
# 6. Copy your Client ID and Secret below

VITE_GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Slack Integration
# 1. Go to https://api.slack.com/apps
# 2. Create a new Slack app
# 3. Add OAuth scopes: chat:write, channels:read
# 4. Install app to workspace
# 5. Copy Bot User OAuth Token below

VITE_SLACK_BOT_TOKEN=xoxb-your-slack-bot-token-here
VITE_SLACK_CHANNEL=#team-updates

# Security Note: In production, use secure token storage and backend proxy
# The application stores tokens securely in memory during session