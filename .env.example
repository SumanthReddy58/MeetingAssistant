# Google Calendar Integration
# Get these from Google Cloud Console: https://console.cloud.google.com/

# Required: Your Google OAuth 2.0 Client ID
VITE_GOOGLE_CLIENT_ID=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

# Optional: Only needed if using authorization code flow (not recommended for frontend)
# For production, handle OAuth server-side instead
VITE_GOOGLE_CLIENT_SECRET=yGOCSPX-m89IZZb9F3nVGXBMwu808EwjPVC9

# Setup Instructions:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Enable the Google Calendar API
# 4. Go to "Credentials" and create OAuth 2.0 Client IDs
# 5. Choose "Web application" as the application type
# 6. Add your domain (e.g., http://localhost:5173 for development) to "Authorized JavaScript origins"
# 7. Add your domain (e.g., http://localhost:5173 for development) to "Authorized redirect URIs"
# 8. For production, add your deployed domain (e.g., https://your-app.netlify.app)
# 9. Copy the Client ID and paste it as VITE_GOOGLE_CLIENT_ID

# Important Notes:
# - Make sure to add both development (localhost:5173) and production URLs
# - The redirect URI must exactly match your domain
# - Enable both Google Calendar API and Google+ API (for user info)