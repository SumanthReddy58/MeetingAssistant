# Google Calendar Integration
# Get these from Google Cloud Console: https://console.cloud.google.com/

# Required: Your Google OAuth 2.0 Client ID
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# Optional: Only needed if using authorization code flow (not recommended for frontend)
# For production, handle OAuth server-side instead
VITE_GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Setup Instructions:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Enable the Google Calendar API
# 4. Go to "Credentials" and create OAuth 2.0 Client IDs
# 5. Add your domain (e.g., https://your-app.netlify.app) to "Authorized JavaScript origins"
# 6. Add your domain (e.g., https://your-app.netlify.app) to "Authorized redirect URIs"
# 7. Copy the Client ID and paste it as VITE_GOOGLE_CLIENT_ID