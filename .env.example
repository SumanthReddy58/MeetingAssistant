# Meeting Assistant Configuration

# This application now works without Google authentication
# All features are available locally without requiring external services

# Optional: If you want to add Google Calendar integration in the future,
# you can configure these variables:
# VITE_GOOGLE_CLIENT_ID=your_google_client_id_here
# VITE_GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# The application stores all data locally in your browser's localStorage
# No external services or authentication required